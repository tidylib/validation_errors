require 'spec_helper'

describe Tidylib::ValidationErrors do
  it 'has a version number' do
    expect(Tidylib::ValidationErrors::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
